// Where the app lives + where published gifts get committed.
// Change these three lines if you fork this repo under another account.

export const REPO_OWNER  = 'Moulin-CM'
export const REPO_NAME   = 'for-you'
export const REPO_BRANCH = 'main'

export const GIFTS_DIR   = 'public/gifts'         // path within the repo
export const SITE_BASE   = `https://${REPO_OWNER.toLowerCase()}.github.io/${REPO_NAME}/`
